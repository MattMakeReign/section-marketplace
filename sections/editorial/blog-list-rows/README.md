# Blog Editorial List Rows

Vertical blog index with a top header block (eyebrow + serif H1) and a stacked list of post rows separated by hairline dividers. Each row is a 3-column layout: thumbnail with hover overlay, serif title paired with mono category/date label, and a sans-serif excerpt.

Motion density: low.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
