# Insights Editorial Carousel

Editorial carousel with a two-row header — eyebrow label, large serif headline left, and prev/next pill controls with a text link right — above a horizontal slider of tall image-led story cards that bleed past the viewport's right edge. Each card exposes an image, title, and body slot.

Motion density: low.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
