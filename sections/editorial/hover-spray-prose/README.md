# Hover Spray Prose

Full-viewport dark statement section with a small spaced eyebrow label above a large centred multi-line prose block. Select words within the prose are underlined as interactive triggers — hovering each word continuously sprays media tiles at that word's position via GSAP spring animations.

Motion density: high.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
