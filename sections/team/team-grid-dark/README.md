# Team Grid Dark

Dark-background team section with a monospace eyebrow and serif headline above a multi-up portrait grid (4-up desktop, 3-up tablet, 2-up mobile). Each card exposes an image slot and a name/role panel with inverted-radius corner cutouts.

Motion density: low.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
