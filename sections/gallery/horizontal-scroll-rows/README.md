# Horizontal Scroll Rows

Pinned multi-row horizontal scroll gallery. Section locks to viewport for several scroll heights; each image row translates horizontally at a rate proportional to its own overflow width. Supports mixed-width image tiles and a statement text/label tile inline with the image rows.

Motion density: experience.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
