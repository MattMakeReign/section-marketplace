# Cursor Magnet Grid

Freeform scattered image grid where each tile carries randomised rotation jitter. Cursor proximity triggers an inverse-square magnetic repulsion — the nearest tile scales up while neighbours are pushed outward via GSAP elastic tweens. No text slots; pure image collection layout.

Motion density: experience.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
