# Sphere Rotate Gallery

Interactive 3D image cluster arranged via Fibonacci spiral distribution on a virtual sphere surface. Mixed-size image tiles and labeled color cards float on a dark background; drag, swipe, or scroll rotates the sphere with inertia easing.

Motion density: experience.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
