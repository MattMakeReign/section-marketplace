# Kinetic Word Reveal

Full-viewport dark hero with a single oversized display word centered on screen. Each letter is an interactive slot — hover expands it into a media tile while neighboring letters spring apart; tile retracts with a tilt-and-pop on mouse-out. No supporting copy or CTA slots.

Motion density: experience.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
