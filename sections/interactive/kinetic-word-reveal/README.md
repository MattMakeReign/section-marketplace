# Kinetic Word Reveal

A large display word where each letter, on hover, swaps to a media tile. Neighbouring letters slide aside to make room with a back-out spring; image holds briefly then retracts with a tiny tilt-and-pop. Built with GSAP tweens.

Motion density: low.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
