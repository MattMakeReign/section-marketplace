# Pricing Three Tier

Centered editorial headline with eyebrow label above a 3-column pricing card grid. Each card exposes a tier name, price, checklist of inclusions, a CTA button, and a footer descriptor line. Third card uses an inverted dark treatment. Below the grid, a full-width inline CTA bar pairs an avatar cluster with a short prompt and a pill button.

Motion density: static.

## Tokens consumed

- Colour: _list the design-system colour tokens this section uses._
- Typography: _list the role-based type tokens this section uses._
- Rhythm: _list the spacing / section-padding tokens._
